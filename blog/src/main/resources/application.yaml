spring:
  application:
    name: blog
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    defer-datasource-initialization: true
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: 123
  h2:
    console:
      enabled: true
#  jpa:
#    # ?? ?? ??
#    show-sql: true
#      properties:
#        hibernate:
#        format_sql: true
#    # ??? ?? ?? data.sql ??
#    defer-datasource-initialization: true